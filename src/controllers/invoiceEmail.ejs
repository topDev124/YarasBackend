<html>

<head>
    <style>
        * {
            font-family: sans-serif;
        }

        p {
            margin: 0px;
            font-size: 18px;
            line-height: 1.5;
        }

        .container {
            max-width: 965px;
            margin: auto;
            padding: 0 2rem;
        }

        .header {
            padding: 2rem;
            background: #f5f5f5;
            text-align: center;
        }

        .logo {
            max-width: 150px;
            width: 100%;
            height: auto;
        }

        .content {
            padding: 1rem 1.5rem;
        }

        .my-2 {
            margin-top: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .my-3 {
            margin-top: 2rem;
            margin-bottom: 2rem;
        }

        .mb-1 {
            margin-bottom: 1rem;
        }

        .mb-2 {
            margin-bottom: 1.5rem;
        }

        .text-center {
            text-align: center;
        }

        .download_btn {
            background: #0bae6b;
            color: #fafafa !important;
            display: inline-block;
            padding: 1rem 2rem;
            font-size: 20px;
            text-decoration: none;
        }

        .footer {
            padding: 1.5rem 1.5rem;
            background: #f5f5f5;
        }

        .footer_logo {
            max-width: 100px;
            width: 100%;
            height: auto;
            margin-bottom: 1.5rem;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <img src="{{logoUrl}}" class="logo" />
        </div>
        <div class="content">
            <h1 class="mb-2">Your Invoice is currently pending payment.</h1>
            <p class="mb-2">Dear {{clientName}},</p>
            <p>Kindly find attached the invoice, Please make the payment using the following link</p>
            <div class="text-center my-3">
                <a class="download_btn" href="{{paymentLink}}" target="_blank">Proceed with payment</a>
            </div>
            <p>Invoice No: #{{invoiceNo}}</p>
            <p>Invoice Date: {{issuedDate}}</p>
            <p>Description:</p>
            <table class="description-table">
                <thead>
                    <tr>
                        <th colspan="3" align="left">Description</th>
                        <th>Quantity</th>
                        <th>Unit price</th>
                        <th align="right">Amount</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each descriptions}}
                    <tr>
                        <td colspan="3">{{content}}</td>
                        <td>{{quantity}}</td>
                        <td>£{{unitPrice}}</td>
                        <td>£{{multiply quantity unitPrice}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>

            <p>Due Date: {{dueDate}}</p>
            <p class="mb-1">Amount Due: £{{totalAmount}}</p>
            <p class="mb-2">Kindly note that if you are doing bank transfer then please ensure that you include the
                invoice number.</p>
            <p>Best regards</p>
            <p>IrVisa.online</p>
        </div>
        <div class="footer">
            <img src="{{logoUrl}}" class="footer_logo" />
            <p>This email may contain confidential information intended solely for the recipient. If you have received
                this email in error, please notify the sender immediately and delete it from your system.</p>
        </div>
    </div>
</body>

</html>